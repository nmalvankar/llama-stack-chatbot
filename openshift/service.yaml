apiVersion: v1
kind: Service
metadata:
  name: llama-stack-chatbot-service
  labels:
    app: llama-stack-chatbot
spec:
  selector:
    app: llama-stack-chatbot
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
